# The pixel coordinates of the cursor at startup, with an example resolution configuration of 1920x1080.
exec_always swaymsg seat - cursor set 1870 540

exec mako 
# exec echo "Xft.dpi: 140" | xrdb -merge
exec "fcitx5 -d"
# exec_always --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
# exec --no-startup-id dbus-update-activation-environment --all

# clipboard manager
## listen for clipboard changes and write to the history
exec "wl-paste --watch cliphist store"

# openrc pipewire
exec gentoo-pipewire-launcher restart &

# update volume signal
# exec sleep 2 && pkill -SIGRTMIN+1 i3blocks
